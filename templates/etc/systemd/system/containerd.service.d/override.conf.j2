[Service]
Environment="HTTP_PROXY={{ http_proxy }}"
Environment="HTTPS_PROXY={{ http_proxy }}"
Environment="http_proxy={{ http_proxy }}"
Environment="https_proxy={{ http_proxy }}"
Environment="no_proxy={{ no_proxy }}"
Environment="NO_PROXY={{ no_proxy }}"
